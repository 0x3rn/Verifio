import { clerkMiddleware } from '@clerk/nextjs/server';

// Route handlers and server-side data access verify authorization again at the
// resource boundary. This proxy only establishes Clerk's request context.
export default clerkMiddleware();

export const config = {
  matcher: [
    '/((?!_next|[^?]*\\.(?:html?|css|js(?!on)|jpe?g|webp|png|gif|svg|ttf|woff2?|ico|csv|docx?|xlsx?|zip|webmanifest)).*)',
    '/(api|trpc)(.*)',
    '/__clerk/(.*)',
  ],
};
